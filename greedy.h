// greedy.h
#ifndef GREEDY_H
#define GREEDY_H

#include <vector>
#include <utility>

std::pair<int, std::vector<int>> knapsackGreedy(int capacity, const std::vector<int>& weights, const std::vector<int>& profits);

#endif#pragma once
